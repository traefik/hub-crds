---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: managedapplications.hub.traefik.io
spec:
  group: hub.traefik.io
  names:
    kind: ManagedApplication
    listKind: ManagedApplicationList
    plural: managedapplications
    singular: managedapplication
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ManagedApplication represents a managed application.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ManagedApplicationSpec describes the ManagedApplication.
            properties:
              apiKeySecrets:
                description: APIKeySecrets references APIKey secrets.
                items:
                  description: APIKeySecret represents a reference to an APIKey secret.
                  properties:
                    name:
                      description: Name of the Secret.
                      maxLength: 253
                      type: string
                  required:
                  - name
                  type: object
                maxItems: 100
                type: array
                x-kubernetes-validations:
                - message: duplicated secrets
                  rule: self.all(x, self.exists_one(y, x.name == y.name))
              appId:
                description: |-
                  AppID is the identifier of the ManagedApplication.
                  It should be unique.
                maxLength: 253
                type: string
              description:
                description: Notes contains .
                type: string
              owner:
                description: |-
                  Owner represents the owner of the ManagedApplication.
                  It should be:
                  - `sub` when using OIDC
                  - `externalID` when using ID
                maxLength: 253
                type: string
            required:
            - apiKeySecrets
            - appId
            - owner
            type: object
          status:
            description: The current status of this ManagedApplication.
            properties:
              hash:
                description: Hash is a hash representing the API.
                type: string
              syncedAt:
                format: date-time
                type: string
              version:
                type: string
            type: object
        type: object
    served: true
    storage: true
